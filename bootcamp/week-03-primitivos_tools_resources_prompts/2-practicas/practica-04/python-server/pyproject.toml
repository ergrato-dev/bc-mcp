[project]
name = "practice-04-all-primitives"
version = "1.0.0"
description = "MCP Server - All 3 primitives combined"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
