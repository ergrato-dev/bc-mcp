[project]
name = "practice-02-resources"
version = "1.0.0"
description = "MCP Server - Resources practice"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
