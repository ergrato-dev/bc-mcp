[project]
name = "practice-03-prompts"
version = "1.0.0"
description = "MCP Server - Prompts practice"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
