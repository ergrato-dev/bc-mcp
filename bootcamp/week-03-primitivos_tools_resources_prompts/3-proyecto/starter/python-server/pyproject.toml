[project]
name = "week03-knowledge-base-server"
version = "1.0.0"
description = "MCP Knowledge Base Server — Semana 03 Proyecto"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
