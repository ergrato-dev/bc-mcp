# CI/CD Pipeline — MCP Library Server (Proyecto Final)
#
# Pipeline completo: lint → test → build → push de imagen Docker a GHCR.
# Este workflow integra todo lo aprendido en la semana 12.
#
# INSTRUCCIONES:
# 1. Implementa los TODO en src/server.py y tests/ primero
# 2. Luego descomenta este workflow sección por sección
# 3. Haz push y verifica que cada job pasa en GitHub Actions

# ============================================
# TODO 1: Nombre del workflow y triggers
# ============================================
# name: CI/CD — MCP Library Server
#
# on:
#   push:
#     branches: [main]
#     tags:
#       - "v*"               # También ejecutar en tags de release
#   pull_request:
#
# permissions:
#   contents: read
#   packages: write


# ============================================
# TODO 2: Job de lint y type checking
# ============================================
# jobs:
#   lint:
#     name: Lint & Type Check
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.13"
#       - name: Cache uv
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/uv
#           key: uv-${{ hashFiles('pyproject.toml') }}
#       - run: pip install --no-cache-dir uv==0.6.6
#       - run: uv sync --frozen --group test
#       - run: uv run ruff check src/ client.py
#       - run: uv run mypy src/ client.py


# ============================================
# TODO 3: Job de tests con cobertura
# ============================================
#   test:
#     name: Tests
#     runs-on: ubuntu-latest
#     needs: lint
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.13"
#       - name: Cache uv
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/uv
#           key: uv-${{ hashFiles('pyproject.toml') }}
#       - run: pip install --no-cache-dir uv==0.6.6
#       - run: uv sync --frozen --group test
#       - run: uv run pytest --cov=src --cov-report=xml
#         env:
#           DB_PATH: ":memory:"
#       - uses: actions/upload-artifact@v4
#         with:
#           name: coverage-report
#           path: coverage.xml


# ============================================
# TODO 4: Job de build y push a GHCR
# ============================================
#   docker:
#     name: Docker Build & Push
#     runs-on: ubuntu-latest
#     needs: test
#     if: github.event_name == 'push'    # Solo en push (no en PRs)
#     steps:
#       - uses: actions/checkout@v4
#       - uses: docker/setup-buildx-action@v3
#       - uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#       - uses: docker/metadata-action@v5
#         id: meta
#         with:
#           images: ghcr.io/${{ github.repository }}
#           tags: |
#             type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
#             type=semver,pattern={{version}}          # v1.2.3 → :1.2.3
#             type=semver,pattern={{major}}.{{minor}}  # v1.2.3 → :1.2
#             type=sha,prefix=sha-
#       - uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: Dockerfile.python
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
