# CI — Python MCP Server
#
# INSTRUCCIONES: Descomenta las secciones indicadas en cada PASO.
# Cada PASO activa una parte del pipeline de CI.
#
# Documentación: https://docs.github.com/en/actions

# ============================================
# PASO 3: Nombre y triggers del workflow
# ============================================
# Descomenta las siguientes líneas:

# name: CI — Python MCP Server
#
# on:
#   push:
#     branches: [main]
#   pull_request:               # También se ejecuta en Pull Requests
#
# permissions:
#   contents: read              # Leer el código del repositorio
#   packages: write             # Publicar imágenes Docker (necesario más adelante)

# ============================================
# PASO 4: Job de lint con ruff y mypy
# ============================================
# Descomenta el bloque completo de jobs:

# jobs:
#   lint:
#     name: Lint & Type Check
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.13"
#
#       - name: Install uv
#         run: pip install --no-cache-dir uv==0.6.6
#
#       - name: Cache uv packages
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/uv
#           key: uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
#           restore-keys: uv-${{ runner.os }}-
#
#       - name: Install dependencies
#         run: uv sync --frozen --group test
#
#       - name: Lint with ruff
#         run: uv run ruff check src/ tests/
#
#       - name: Type check with mypy
#         run: uv run mypy src/

# ============================================
# PASO 4 (continuación): Job de tests
# ============================================
# Descomenta el job de tests (dentro del bloque `jobs:` de arriba):

#   test:
#     name: Tests with coverage
#     runs-on: ubuntu-latest
#     needs: lint                   # Solo corre si lint pasa
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.13"
#
#       - name: Install uv
#         run: pip install --no-cache-dir uv==0.6.6
#
#       - name: Cache uv packages
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/uv
#           key: uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
#           restore-keys: uv-${{ runner.os }}-
#
#       - name: Install dependencies with test group
#         run: uv sync --frozen --group test
#
#       - name: Run tests with coverage
#         run: uv run pytest --cov=src --cov-report=term-missing
#         env:
#           DB_PATH: ":memory:"     # SQLite in-memory para tests en CI
