# Docker Build & Push to GHCR
#
# INSTRUCCIONES: Descomenta las secciones indicadas en cada PASO.
# Este workflow extiende el CI agregando build y push de imagen Docker.

# ============================================
# PASO 3: Nombre, triggers y permisos
# ============================================
# Descomenta las siguientes líneas:

# name: Docker Build & Push — GHCR
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#
# permissions:
#   contents: read
#   packages: write     # Necesario para push a GHCR

# ============================================
# PASO 3 (continuación): Jobs lint y test
# ============================================
# (Copia los jobs lint y test de la práctica 01 aquí)

# jobs:
#   lint:
#     name: Lint & Type Check
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.13"
#       - run: pip install --no-cache-dir uv==0.6.6
#       - run: uv sync --frozen --group test
#       - run: uv run ruff check src/
#
#   test:
#     name: Tests
#     runs-on: ubuntu-latest
#     needs: lint
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.13"
#       - run: pip install --no-cache-dir uv==0.6.6
#       - run: uv sync --frozen --group test
#       - run: uv run pytest
#         env:
#           DB_PATH: ":memory:"

# ============================================
# PASO 4: Job docker — build y push a GHCR
# ============================================
# Descomenta el siguiente job:

#   docker:
#     name: Docker Build & Push
#     runs-on: ubuntu-latest
#     needs: test                     # Solo si los tests pasan
#     # Solo en push a main, NO en pull_request
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3
#
#       - name: Login to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}   # Token automático de GitHub
#
#       - name: Extract Docker metadata (tags y labels)
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ghcr.io/${{ github.repository }}
#           tags: |
#             type=raw,value=latest          # :latest en cada push a main
#             type=sha,prefix=sha-           # :sha-abc1234 (trazabilidad)
#
#       - name: Build and push image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: Dockerfile.python
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha            # Caché de capas Docker en GitHub Actions
#           cache-to: type=gha,mode=max
