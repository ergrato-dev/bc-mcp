[project]
name = "practice-04-context-logging"
version = "1.0.0"
description = "Week 04 — Practice 04: Context, Logging, and MCP Inspector"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
