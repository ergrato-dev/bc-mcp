[project]
name = "practice-01-fastmcp-basic"
version = "1.0.0"
description = "Week 04 — Practice 01: First FastMCP Server"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
