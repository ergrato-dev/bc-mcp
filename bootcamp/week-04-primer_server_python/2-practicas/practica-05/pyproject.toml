[project]
name = "practice-05-multiple-tools"
version = "1.0.0"
description = "Week 04 — Practice 05: Multiple Tools with Different Types"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
