[project]
name = "practice-02-type-hints-schema"
version = "1.0.0"
description = "Week 04 — Practice 02: Type Hints and Auto Schema"
requires-python = ">=3.13"
dependencies = [
    "mcp==1.9.0",
    "pydantic==2.11.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
